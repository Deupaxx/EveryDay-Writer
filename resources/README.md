# Resources Folder

This folder contains background writing references that may be useful while maintaining Everyday Writer. It is not part of the runtime dispatch chain.

Only the explicitly named writing reference files in `package.json` are shipped to npm.

Local files named `Untitled document*.md` are private import clutter or working notes. They are ignored by git and must not be added to the package whitelist.
