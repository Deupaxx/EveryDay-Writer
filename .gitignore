drafts/
dist/
node_modules/
resources/Untitled document*.md

# Voice profiles belong in ~/.everyday-writer/, never in the repo. This is a
# backstop in case a path ever resolves relative to the working directory —
# client voice profiles must not reach a public repository.
.everyday-writer/
voices/
